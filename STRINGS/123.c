// Inversor e trocador de letras

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <strlen.h>

int main()
{
    char palavra[10];
    int maior = 0;
    int tamanho = strlen(palavra);
    printf("Digite a palavra:\n");
    scanf("%s", palavra);



    return 0;
}
